import styled from 'styled-components';

const HomepageWrapper = styled.div`

    .Home{
        height: 10px;
        width: 10px;
        background: #000;
    }

`;

export default HomepageWrapper;